[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "revenue-captain"
version = "0.1.0"
description = "Normalizes applications/events/school-routes into durable state and a ranked next-action queue."
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.setuptools]
packages = ["revenue_captain"]
